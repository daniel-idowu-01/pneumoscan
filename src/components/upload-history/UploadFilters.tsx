const UploadFilters = () => {
  return (
    <div className="bg-surface-dark rounded-xl p-5 border border-border-dark flex flex-col gap-5">
      {/* Search + Date + Export */}
      {/* Filters row */}
      {/* You can later split these if they get interactive */}
    </div>
  )
}

export default UploadFilters
